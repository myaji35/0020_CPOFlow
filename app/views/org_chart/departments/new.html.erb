<% content_for :title, "부서 추가 — #{@company.name}" %>

<div class="max-w-2xl mx-auto py-8 px-4">
  <div class="flex items-center gap-2 text-sm text-gray-400 mb-6">
    <%= link_to "조직도", org_chart_path, class: "hover:text-primary" %>
    <span>/</span>
    <span><%= @company.name %></span>
    <span>/</span>
    <span>부서 추가</span>
  </div>

  <div class="bg-white rounded-xl border border-gray-200 p-6">
    <h1 class="text-lg font-semibold text-gray-900 mb-1">부서 추가</h1>
    <p class="text-sm text-gray-500 mb-6"><%= @company.name %></p>
    <%= render "form", department: @department %>
  </div>
</div>
