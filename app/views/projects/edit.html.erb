<% content_for :title, "프로젝트 수정 - #{@project.name}" %>
<div class="max-w-2xl">
  <div class="mb-6">
    <div class="flex items-center gap-2 text-sm text-gray-400 mb-1">
      <%= link_to "프로젝트", projects_path, class: "hover:text-primary" %>
      <span>/</span>
      <%= link_to @project.name, @project, class: "hover:text-primary" %>
      <span>/</span><span>수정</span>
    </div>
    <h1 class="text-2xl font-bold text-gray-900">프로젝트 수정</h1>
  </div>
  <div class="bg-white rounded-xl border border-gray-100 shadow-sm p-6">
    <%= render "form" %>
  </div>
</div>
