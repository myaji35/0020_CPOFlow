<% content_for :title, "프로젝트 관리" %>

<div class="flex flex-col gap-6">
  <!-- Header -->
  <div class="flex items-center justify-between">
    <div>
      <h1 class="text-2xl font-bold text-gray-900 dark:text-white">현장/프로젝트 관리</h1>
      <p class="text-sm text-gray-500 dark:text-gray-500 mt-1">원전·수력·터널·GTX 현장별 예산 집행률 관리</p>
    </div>
    <%= link_to new_project_path, class: "inline-flex items-center gap-2 bg-primary text-white px-4 py-2 rounded-lg text-sm font-medium hover:bg-primary-dark transition-colors" do %>
      <svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
      새 프로젝트
    <% end %>
  </div>

  <!-- KPI 통계 카드 (4칸) -->
  <div class="grid grid-cols-4 gap-4">
    <div class="bg-white dark:bg-gray-800 rounded-xl border border-gray-200 dark:border-gray-700 p-4 shadow-sm">
      <div class="flex items-center gap-2 mb-1">
        <svg class="w-4 h-4 text-gray-400" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>
        <p class="text-xs text-gray-500 dark:text-gray-500 font-medium">총 현장 수</p>
      </div>
      <p class="text-2xl font-bold text-gray-900 dark:text-white"><%= @total_count %>개</p>
      <p class="text-xs text-gray-500 dark:text-gray-500 mt-1">활성 <%= @active_count %>개</p>
    </div>
    <div class="bg-white dark:bg-gray-800 rounded-xl border border-gray-200 dark:border-gray-700 p-4 shadow-sm">
      <div class="flex items-center gap-2 mb-1">
        <svg class="w-4 h-4 text-primary" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg>
        <p class="text-xs text-gray-500 dark:text-gray-500 font-medium">활성 주문 연결</p>
      </div>
      <p class="text-2xl font-bold text-primary"><%= number_with_delimiter(@active_order_count) %>건</p>
      <p class="text-xs text-gray-500 dark:text-gray-500 mt-1">진행중 현장의 주문</p>
    </div>
    <div class="bg-white dark:bg-gray-800 rounded-xl border border-gray-200 dark:border-gray-700 p-4 shadow-sm">
      <div class="flex items-center gap-2 mb-1">
        <svg class="w-4 h-4 text-green-500" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/></svg>
        <p class="text-xs text-gray-500 dark:text-gray-500 font-medium">총 집행금액</p>
      </div>
      <p class="text-2xl font-bold text-green-600 dark:text-green-400">$<%= number_with_delimiter(@total_utilized.round.to_i) %></p>
      <% if @total_budget > 0 %>
        <p class="text-xs text-gray-500 dark:text-gray-500 mt-1">예산 대비 <%= (@total_utilized / @total_budget * 100).round(1) %>%</p>
      <% end %>
    </div>
    <div class="bg-white dark:bg-gray-800 rounded-xl border border-gray-200 dark:border-gray-700 p-4 shadow-sm">
      <div class="flex items-center gap-2 mb-1">
        <svg class="w-4 h-4 text-orange-500" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="20" x2="18" y2="10"/><line x1="12" y1="20" x2="12" y2="4"/><line x1="6" y1="20" x2="6" y2="14"/></svg>
        <p class="text-xs text-gray-500 dark:text-gray-500 font-medium">평균 주문/현장</p>
      </div>
      <p class="text-2xl font-bold text-orange-600 dark:text-orange-400"><%= @avg_orders_per_project %>건</p>
      <p class="text-xs text-gray-500 dark:text-gray-500 mt-1">현장당 평균 발주</p>
    </div>
  </div>

  <!-- 검색 + 활성 필터 -->
  <div class="flex items-center gap-3">
    <%= form_with url: projects_path, method: :get, local: true, class: "flex-1 flex items-center gap-2" do %>
      <div class="relative flex-1 max-w-md">
        <svg class="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-gray-400" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
        <input type="text" name="q" value="<%= params[:q] %>" placeholder="이름, 위치, 코드로 검색..."
               class="w-full pl-9 pr-3 py-2.5 border border-gray-300 rounded-lg text-sm text-gray-900 dark:text-white dark:bg-gray-800 dark:border-gray-600 placeholder-gray-400 focus:outline-none focus:border-[#00A1E0] focus:ring-1 focus:ring-[#00A1E0]">
      </div>
      <% if params[:category].present? %><input type="hidden" name="category" value="<%= params[:category] %>"><% end %>
      <% if params[:status].present? %><input type="hidden" name="status" value="<%= params[:status] %>"><% end %>
      <label class="flex items-center gap-2 text-sm text-gray-600 dark:text-gray-400 cursor-pointer select-none">
        <input type="checkbox" name="active_only" value="1" <%= params[:active_only] == "1" ? "checked" : "" %>
               onchange="this.form.submit()"
               class="w-4 h-4 rounded border-gray-300 text-primary focus:ring-primary">
        활성만
      </label>
      <button type="submit" class="bg-primary text-white px-4 py-2.5 rounded-lg text-sm font-medium hover:bg-primary-dark transition-colors">검색</button>
      <% if params[:q].present? || params[:active_only] == "1" %>
        <%= link_to "초기화", projects_path(category: params[:category], status: params[:status]), class: "text-sm text-gray-500 hover:text-primary" %>
      <% end %>
    <% end %>
  </div>

  <!-- 카테고리 + 상태 필터 -->
  <div class="flex gap-2 flex-wrap items-center">
    <div class="flex gap-2 flex-wrap">
      <%= link_to "전체", projects_path(status: params[:status]), class: "px-4 py-2 text-sm rounded-full #{params[:category].blank? ? 'bg-primary text-white' : 'bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-600 text-gray-600 dark:text-gray-500 hover:border-primary'}" %>
      <% Project::SITE_CATEGORIES.each do |cat| %>
        <% label = { "nuclear"=>"원전","hydro"=>"수력","tunnel"=>"터널","gtx"=>"GTX","general"=>"일반" }[cat] || cat %>
        <%= link_to label, projects_path(category: cat, status: params[:status]), class: "px-4 py-2 text-sm rounded-full #{params[:category] == cat ? 'bg-primary text-white' : 'bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-600 text-gray-600 dark:text-gray-500 hover:border-primary'}" %>
      <% end %>
    </div>
    <div class="w-px h-6 bg-gray-200 dark:bg-gray-700"></div>
    <div class="flex gap-2 flex-wrap">
      <%= link_to "전체 상태", projects_path(category: params[:category]), class: "px-3 py-1.5 text-xs rounded-full #{params[:status].blank? ? 'bg-gray-700 text-white dark:bg-gray-300 dark:text-gray-900' : 'bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-600 text-gray-600 dark:text-gray-500 hover:border-gray-400'}" %>
      <% [["진행중", "active", "text-green-700 bg-green-50 dark:bg-green-900/30 dark:text-green-400"], ["계획", "planning", "text-yellow-700 bg-yellow-50 dark:bg-yellow-900/30 dark:text-yellow-400"], ["완료", "completed", "text-gray-600 bg-gray-100 dark:bg-gray-700 dark:text-gray-500"], ["중단", "suspended", "text-red-700 bg-red-50 dark:bg-red-900/30 dark:text-red-400"]].each do |label, val, cls| %>
        <%= link_to label, projects_path(category: params[:category], status: val), class: "px-3 py-1.5 text-xs rounded-full font-medium #{params[:status] == val ? cls : 'bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-600 text-gray-600 dark:text-gray-500 hover:border-gray-400'}" %>
      <% end %>
    </div>
  </div>

  <!-- 현장 유형별 발주 비중 파이차트 -->
  <% category_stats = Project::SITE_CATEGORIES.map do |cat|
    projs = @projects.select { |p| p.site_category == cat }
    { cat: cat, label: { "nuclear"=>"원전","hydro"=>"수력","tunnel"=>"터널","gtx"=>"GTX","general"=>"일반" }[cat],
      count: projs.sum { |p| p.orders.count },
      value: projs.sum { |p| p.budget_utilized } }
  end.select { |s| s[:count] > 0 } %>
  <% total_orders = category_stats.sum { |s| s[:count] }; total_value = category_stats.sum { |s| s[:value] } %>

  <% if total_orders > 0 %>
    <div class="grid grid-cols-2 gap-4">
      <!-- 파이차트 (SVG) -->
      <div class="bg-white dark:bg-gray-800 rounded-xl border border-gray-200 dark:border-gray-700 shadow-sm p-5">
        <h3 class="text-sm font-semibold text-gray-700 dark:text-gray-300 mb-4">현장 유형별 발주 비중</h3>
        <%
          colors = ["#1E3A5F","#00A1E0","#38BDF8","#7DD3FC","#BAE6FD"]
          cumulative = 0
          pie_segments = category_stats.each_with_index.map do |s, i|
            pct = total_orders > 0 ? (s[:count].to_f / total_orders) : 0
            seg = { pct: pct, color: colors[i % colors.size], label: s[:label], count: s[:count], value: s[:value], start: cumulative }
            cumulative += pct
            seg
          end
        %>
        <div class="flex items-center gap-6">
          <svg viewBox="0 0 100 100" class="w-32 h-32 shrink-0 -rotate-90">
            <% radius = 40; cx = 50; cy = 50; stroke = 20 %>
            <% angle = 0 %>
            <% pie_segments.each do |seg| %>
              <% start_angle = angle * 2 * Math::PI
                 sweep = seg[:pct] * 2 * Math::PI
                 x1 = cx + radius * Math.cos(start_angle); y1 = cy + radius * Math.sin(start_angle)
                 x2 = cx + radius * Math.cos(start_angle + sweep); y2 = cy + radius * Math.sin(start_angle + sweep)
                 large = sweep > Math::PI ? 1 : 0
                 angle += seg[:pct] %>
              <path d="M <%= cx %> <%= cy %> L <%= x1.round(2) %> <%= y1.round(2) %> A <%= radius %> <%= radius %> 0 <%= large %> 1 <%= x2.round(2) %> <%= y2.round(2) %> Z"
                    fill="<%= seg[:color] %>" opacity="0.9"/>
            <% end %>
            <circle cx="<%= cx %>" cy="<%= cy %>" r="20" fill="white" class="dark:fill-gray-800"/>
          </svg>
          <!-- 범례 -->
          <div class="space-y-2 flex-1">
            <% pie_segments.each_with_index do |seg, i| %>
              <div class="flex items-center justify-between text-xs">
                <div class="flex items-center gap-2">
                  <div class="w-3 h-3 rounded-sm shrink-0" style="background-color: <%= seg[:color] %>"></div>
                  <span class="text-gray-600 dark:text-gray-500"><%= seg[:label] %></span>
                </div>
                <div class="flex items-center gap-3">
                  <span class="font-medium text-gray-900 dark:text-white"><%= seg[:count] %>건</span>
                  <span class="text-gray-500 dark:text-gray-500"><%= (seg[:pct] * 100).round(1) %>%</span>
                </div>
              </div>
            <% end %>
            <div class="pt-2 border-t border-gray-200 dark:border-gray-700 flex justify-between text-xs font-semibold text-gray-700 dark:text-gray-300">
              <span>합계</span><span><%= total_orders %>건 | $<%= number_with_delimiter(total_value.round.to_i) %></span>
            </div>
          </div>
        </div>
      </div>

      <!-- 현장 유형별 발주 현황 카드 -->
      <div class="grid grid-cols-2 gap-3 content-start">
        <% category_stats.each_with_index do |s, i| %>
          <div class="bg-white dark:bg-gray-800 rounded-xl border border-gray-200 dark:border-gray-700 shadow-sm p-4">
            <div class="flex items-center gap-2 mb-2">
              <div class="w-3 h-3 rounded-sm" style="background-color: <%= colors[i % colors.size] %>"></div>
              <span class="text-xs font-semibold text-gray-600 dark:text-gray-500"><%= s[:label] %></span>
            </div>
            <p class="text-xl font-bold text-gray-900 dark:text-white"><%= s[:count] %>건</p>
            <p class="text-xs text-gray-500 dark:text-gray-500">$<%= number_with_delimiter(s[:value].round.to_i) %></p>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>

  <!-- 테이블 -->
  <div class="bg-white dark:bg-gray-800 rounded-xl border border-gray-200 dark:border-gray-700 shadow-sm overflow-hidden">
    <table class="w-full">
      <thead class="bg-gray-50 dark:bg-gray-700/50 border-b border-gray-200 dark:border-gray-700">
        <tr>
          <th class="text-left px-4 py-3 text-xs font-semibold text-gray-500 dark:text-gray-500 uppercase">프로젝트명</th>
          <th class="text-left px-4 py-3 text-xs font-semibold text-gray-500 dark:text-gray-500 uppercase">발주처</th>
          <th class="text-left px-4 py-3 text-xs font-semibold text-gray-500 dark:text-gray-500 uppercase">현장 유형</th>
          <th class="text-left px-4 py-3 text-xs font-semibold text-gray-500 dark:text-gray-500 uppercase">상태</th>
          <th class="text-right px-4 py-3 text-xs font-semibold text-gray-500 dark:text-gray-500 uppercase">오더 건수</th>
          <th class="text-right px-4 py-3 text-xs font-semibold text-gray-500 dark:text-gray-500 uppercase">발주 금액</th>
          <th class="px-4 py-3"></th>
        </tr>
      </thead>
      <tbody class="divide-y divide-gray-50 dark:divide-gray-700">
        <% @projects.each do |proj| %>
          <tr class="hover:bg-gray-50 dark:hover:bg-gray-700/50 transition-colors">
            <td class="px-4 py-3">
              <div class="font-medium text-gray-900 dark:text-white"><%= proj.name %></div>
              <div class="text-xs text-gray-500 dark:text-gray-500"><%= proj.location %></div>
            </td>
            <td class="px-4 py-3 text-sm text-gray-600 dark:text-gray-500"><%= proj.client.name %></td>
            <td class="px-4 py-3">
              <span class="inline-flex px-2 py-0.5 text-xs font-medium rounded-full bg-blue-50 dark:bg-blue-900/30 text-blue-700 dark:text-blue-400"><%= proj.site_category_label %></span>
            </td>
            <td class="px-4 py-3">
              <% status_class = { "active"=>"bg-green-600 text-white dark:bg-green-900/30 dark:text-green-400","planning"=>"bg-yellow-500 text-white dark:bg-yellow-900/30 dark:text-yellow-400","completed"=>"bg-gray-500 text-white dark:bg-gray-700 dark:text-gray-500","suspended"=>"bg-red-600 text-white dark:bg-red-900/30 dark:text-red-400" }[proj.status] %>
              <span class="inline-flex px-2 py-0.5 text-xs font-medium rounded-full <%= status_class %>"><%= proj.status_label %></span>
            </td>
            <td class="px-4 py-3 text-right text-sm font-medium text-gray-900 dark:text-white"><%= proj.orders.count %>건</td>
            <td class="px-4 py-3 text-right text-sm font-medium text-primary">
              $<%= number_with_delimiter(proj.budget_utilized.round.to_i) %>
            </td>
            <td class="px-4 py-3 text-right">
              <%= link_to "상세", proj, class: "text-xs text-primary hover:underline" %>
            </td>
          </tr>
        <% end %>
        <% if @projects.empty? %>
          <tr><td colspan="7" class="px-4 py-12 text-center text-gray-500 dark:text-gray-500 text-sm">등록된 프로젝트가 없습니다.</td></tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
