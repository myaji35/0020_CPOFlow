<%# ISS-046 후속: 칸반 컬럼 백그라운드 추가 페치 응답 %>
<%# locals: status, orders %>
<turbo-frame id="kanban-more-<%= status %>" class="contents">
  <% orders.each do |order| %>
    <%= render "kanban/card", order: order %>
  <% end %>
</turbo-frame>
