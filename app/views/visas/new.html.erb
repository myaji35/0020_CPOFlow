<% content_for :title, t("visas.new.title") %>
<div class="flex flex-col gap-4">
  <div class="flex items-center gap-2 text-sm text-gray-500">
    <%= link_to t("nav.employees"), employees_path, class: "hover:text-primary" %>
    <span>/</span>
    <%= link_to @employee.name, @employee, class: "hover:text-primary" %>
    <span>/</span>
    <span><%= t("visas.new.title") %></span>
  </div>
  <h1 class="text-2xl font-bold text-gray-900 dark:text-white"><%= t("visas.new.title") %></h1>
  <div class="bg-white dark:bg-gray-800 rounded-xl border border-gray-200 dark:border-gray-700 shadow-sm p-6">
    <%= render "form", visa: @visa %>
  </div>
</div>
