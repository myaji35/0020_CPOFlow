<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    body { font-family: -apple-system, sans-serif; color: #1a1a2e; background: #f3f2f2; margin: 0; padding: 20px; }
    .container { max-width: 600px; margin: 0 auto; background: white; border-radius: 8px; overflow: hidden; box-shadow: 0 2px 8px rgba(0,0,0,0.1); }
    .header { background: <%= @days_remaining <= 3 ? '#D93025' : (@days_remaining <= 7 ? '#F4A83A' : '#1E3A5F') %>; color: white; padding: 24px 32px; }
    .header h1 { margin: 0; font-size: 20px; font-weight: 700; }
    .header p { margin: 4px 0 0; opacity: 0.85; font-size: 14px; }
    .body { padding: 32px; }
    .badge { display: inline-block; padding: 4px 12px; border-radius: 4px; font-size: 12px; font-weight: 600; background: <%= @days_remaining == 0 ? '#D93025' : (@days_remaining <= 3 ? '#F4A83A' : '#00A1E0') %>; color: white; margin-bottom: 16px; }
    .order-card { border: 1px solid #e5e5e5; border-radius: 6px; padding: 16px 20px; margin: 16px 0; }
    .order-card h2 { margin: 0 0 8px; font-size: 16px; color: #1E3A5F; }
    .order-card .meta { font-size: 13px; color: #666; }
    .order-card .meta span { margin-right: 16px; }
    .cta { display: block; margin: 24px 0 0; padding: 12px 24px; background: #00A1E0; color: white; text-decoration: none; border-radius: 6px; font-weight: 600; text-align: center; }
    .footer { border-top: 1px solid #f0f0f0; padding: 16px 32px; font-size: 12px; color: #999; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>CPOFlow — Delivery Reminder</h1>
      <p><%= @urgency %></p>
    </div>
    <div class="body">
      <span class="badge">
        <%= @days_remaining == 0 ? "DUE TODAY" : "D-#{@days_remaining}" %>
      </span>

      <p>Hi <strong><%= @user.name %></strong>,</p>
      <p>The following order requires your attention — it is due <strong><%= @days_remaining == 0 ? "TODAY" : "in #{@days_remaining} days" %></strong>.</p>

      <div class="order-card">
        <h2><%= @order.title %></h2>
        <div class="meta">
          <span>Customer: <%= @order.customer_name %></span>
          <span>Due: <%= @order.due_date&.strftime("%d %b %Y") || "—" %></span>
          <span>Status: <%= Order::STATUS_LABELS[@order.status] %></span>
        </div>
        <% if @order.item_name.present? %>
          <p style="margin: 8px 0 0; font-size: 13px; color: #555;">Items: <%= @order.item_name %></p>
        <% end %>
      </div>

      <a href="<%= order_url(@order) %>" class="cta">View Order in CPOFlow</a>
    </div>
    <div class="footer">
      You received this notification because you are assigned to this order in CPOFlow.<br>
      AtoZ2010 Inc. — Abu Dhabi, UAE
    </div>
  </div>
</body>
</html>
