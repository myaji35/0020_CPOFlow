<% content_for :title, "자격증 수정" %>
<div class="flex flex-col gap-4">
  <div class="flex items-center gap-2 text-sm text-gray-400">
    <%= link_to @employee.name, @employee, class: "hover:text-primary" %>
    <span>/</span><span>자격증 수정</span>
  </div>
  <h1 class="text-2xl font-bold text-gray-900 dark:text-white">자격증 수정</h1>
  <div class="bg-white dark:bg-gray-800 rounded-xl border border-gray-100 dark:border-gray-700 shadow-sm p-6">
    <%= render "form", certification: @certification %>
  </div>
</div>
