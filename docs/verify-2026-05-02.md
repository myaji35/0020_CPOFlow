# 운영 검증 보고 — ISS-293/296/297/300 (2026-05-02)

## 환경
- URL: https://cpoflow.ddtl.co.kr
- 검증 시각: 2026-05-01 19:24 UTC (KST 2026-05-02 04:24)
- 서버: Vultr 158.247.235.31, Rails production
- 방식: `kamal app exec --reuse "bin/rails runner ..."` — 직접 모델/Job 조작
- 운영 데이터 영구 변경 없음 (모든 더미 데이터 destroy 완료)

---

## ISS-293 RFP 자동 분석

**결과: PASS**

| 항목 | 값 | PASS/FAIL |
|---|---|---|
| `Rfp::AnalyzeAttachmentsJob` 클래스 존재 | true | PASS |
| Solid Queue 접근 가능 | true | PASS |
| `Task.auto_generated` 컬럼 존재 | true | PASS |
| `perform_later` 호출 시 SolidQueue에 enqueue | 0 → 1건 (Rfp::AnalyzeAttachmentsJob, queue=default) | PASS |
| 첨부 없는 오더 → `rfp_analysis_state = "skipped"` | skipped | PASS |
| KanbanController 소스: new_rfq → make_quo 전환 시 enqueue 코드 확인 | 코드 직접 확인 | PASS |

### 현황
- 운영 new_rfq 오더: **11,837건**, 이 중 첨부 있는 건: **2,209건**
- 기존 분석 완료(done): **0건** — 아직 실제 드래그 이동으로 Job이 발동된 케이스 없음
- Job enqueue 로직(코드): `if old_status == "new_rfq" && @order.status == "make_quo" → Rfp::AnalyzeAttachmentsJob.perform_later(@order.id)` 정상 배치
- 실제 enqueue 테스트: 더미 오더 생성 → status 변경 → `perform_later` 호출 → SolidQueue 1건 등록 확인

### 비고
- `rfp_analyzed_at` 24시간 중복 방지 로직 정상
- 첨부 없을 때 `skipped`, 텍스트 추출 실패 시 fallback 태스크 생성 로직 포함

---

## ISS-296 퇴사자 잠금

**결과: PASS (4 케이스 모두 통과)**

| 케이스 | 내용 | 기대 | 실제 | PASS/FAIL |
|---|---|---|---|---|
| 초기 상태 | User/Employee 생성, email 자동 연결 | active=true, auth=true | active=true, auth=true | PASS |
| Case 1 | Employee.active=false | User.active=false, auth 차단 | User.active=false, auth 차단, inactive_message="account_deactivated" | PASS |
| Case 2 | Employee.active=true 복원 | User.active=true, auth 허용 | active=true, auth=true | PASS |
| Case 3 | termination_date=30일 전 (과거) | User.active=false, auth 차단 | User.active=false, auth 차단 | PASS |
| Case 4 | termination_date=30일 후 (미래, active=true) | User.active=true, auth 허용 | active=true, auth=true | PASS |

### 핵심 로직 확인
- `Employee#sync_user_account_status`: `active && (termination_date.nil? || termination_date > Date.current)` → `user.update_column(:active, ...)`
- `User#active_for_authentication?`: `super && active` — Devise 로그인 차단
- `User#inactive_message`: `:account_deactivated` 반환

---

## ISS-297 HR 첨부파일

**결과: PASS (6 케이스 모두 통과)**

`DocumentAttachable` concern이 Employee, Visa, EmploymentContract, Certification 4모델에 정상 적용.

| 케이스 | 모델 | 파일 | 기대 | 실제 | PASS/FAIL |
|---|---|---|---|---|---|
| 1 | Employee | PDF 10KB | 첨부 허용 | valid=true, attached=true | PASS |
| 2 | Employee | EXE 파일 | 거부 (extension 차단) | valid=false, errors에 "exe" 포함 | PASS |
| 3 | Visa | PDF 5KB | 첨부 허용 | valid=true, attached=true | PASS |
| 4 | EmploymentContract | PDF 8KB | 첨부 허용 | valid=true, attached=true | PASS |
| 5 | Certification | PDF 3KB | 첨부 허용 | valid=true, attached=true | PASS |
| 6 | Employee | PDF 26MB (초과) | 거부 (크기 초과) | valid=false, errors에 "초과" 포함 | PASS |

### 핵심 로직 확인
- `DENY_EXTENSIONS`: exe, bat, sh 등 40종 거부
- `MAX_SIZE`: 25MB 초과 거부
- `ALLOWED_CONTENT_TYPES`: application/pdf 등 정상 MIME 허용
- 모든 더미 blob purge_later로 정리 완료

---

## ISS-300 viewer 차단

**결과: PASS (4 컨트롤러 모두 확인)**

| 컨트롤러 | 접근 제어 | viewer 차단 | PASS/FAIL |
|---|---|---|---|
| `EmploymentContractsController` | `before_action :require_manager!` (전체) | viewer.admin_or_manager? = false → redirect | PASS |
| `VisasController` | `before_action :require_manager!` (전체) | viewer.admin_or_manager? = false → redirect | PASS |
| `Settings::CardStatusesController` | `before_action :require_manager!` (전체) | viewer.admin_or_manager? = false → redirect | PASS |
| `ContactPersonsController` | `before_action :require_member!, only: [:new, :create, :edit, :update, :destroy, :create_from_signature]` | viewer.member? = false → redirect/403 | PASS |

### 접근 제어 메서드 검증
- `require_manager!`: `current_user.admin_or_manager?` → false면 `redirect_to root_path, alert: "Access denied."`
- `require_member!`: `admin? || manager? || member?` 중 하나도 없으면 → HTML: redirect, JSON: 403
- viewer(role=0)는 위 조건 모두 불충족 → 차단 확정
- viewer는 **로그인 자체는 가능** (`active_for_authentication?` = true) — 단, 민감 화면 접근 차단됨

---

## 종합

| ISS | 기능 | 케이스 수 | PASS | FAIL |
|---|---|---|---|---|
| ISS-293 | RFP 자동 분석 | 6 | 6 | 0 |
| ISS-296 | 퇴사자 잠금 | 5 | 5 | 0 |
| ISS-297 | HR 첨부파일 | 6 | 6 | 0 |
| ISS-300 | viewer 차단 | 4 | 4 | 0 |
| **합계** | | **21** | **21** | **0** |

**전체 PASS: 21/21**

### 문제 발견: 없음

### 참고 사항 (결함은 아님)
- ISS-293: 운영 환경에서 실제 칸반 드래그(new_rfq → make_quo)가 아직 발생하지 않아 `rfp_analysis_state="done"` 오더 없음. Job 자체는 정상 enqueue/동작 확인. 첫 번째 드래그 후 Opus API 키 설정 여부에 따라 실제 분석 성공 여부가 결정됨.
- ISS-297: `.exe` 등 DENY_EXTENSIONS 파일은 `content_type: "application/octet-stream"`으로 업로드해도 확장자 기반으로 차단됨.

### 권장 후속 조치
- ISS-293: 테스트 환경에서 실제 첨부 있는 new_rfq 오더를 make_quo로 이동 → Anthropic API 키 설정 후 end-to-end 분석 완료 확인 권장.
